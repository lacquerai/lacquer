version: "2.0"  # Invalid version

metadata:
  name: 123  # Should be string, not number

agents:
  bad_agent:
    provider: openai
    model: "invalid-model-name"
    temperature: 5.0  # Should be <= 2.0
    max_tokens: -100  # Should be positive
    top_p: 1.5  # Should be <= 1.0

workflow:
  steps: []  # Empty steps array should fail minimum validation
  
  # Missing required fields
  outputs:
    result: 123  # Should be string for template