version: "1.0"

metadata:
  name: "undefined-var-position-test"

agents:
  test_agent:
    provider: openai
    model: "gpt-4"

workflow:
  steps:
    - id: step1
      agent: test_agent
      prompt: "Use undefined variable: {{ undefined.variable }}"