version: 1.0  # Should be string, not number

metadata:
  name: "type-mismatches-test"
  version: 123  # Should be string

agents:
  bad_agent:
    model: 123  # Should be string
    temperature: "0.7"  # Should be number
    max_tokens: "1000"  # Should be number
    tools: "invalid"  # Should be array

workflow:
  inputs:
    test_input:
      type: 123  # Should be string
      required: "true"  # Should be boolean
      
  steps:
    - id: 123  # Should be string
      agent: "bad_agent"
      prompt: 456  # Should be string
      condition: 789  # Should be string
      
    - id: "another_step"
      agent: "bad_agent"
      prompt: "test"
      with: "should_be_object"  # Should be object
      
  outputs: "should_be_object"  # Should be object