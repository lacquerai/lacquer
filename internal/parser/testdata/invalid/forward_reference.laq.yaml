version: "1.0"

metadata:
  name: "forward-reference-test"

agents:
  agent1:
    provider: openai
    model: "gpt-4"

workflow:
  steps:
    - id: "step1"
      agent: "agent1"
      prompt: "Use output from future step: {{ steps.step2.output }}"
      
    - id: "step2"
      agent: "agent1"
      prompt: "Generate some output"