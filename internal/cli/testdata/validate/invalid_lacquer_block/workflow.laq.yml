version: "1.0"
metadata:
  name: invalid-lacquer-block-test
  description: Test workflow with invalid lacquer block references

agents:
  agent1:
    uses: lacquer/invalid-name!  # Invalid character
  
  agent2:
    uses: lacquer/123-starts-with-number  # Invalid - starts with number
  
  agent3:
    uses: lacquer/has spaces  # Invalid - contains spaces

workflow:
  steps:
    - id: step1
      uses: lacquer/missing-version  # Valid but will be checked
      
    - id: step2
      uses: lacquer/valid-block@v2.0.1  # Valid format