version: "1.0"
metadata:
  name: invalid-runtime-test
  description: Test workflow with invalid runtime requirements

requirements:
  runtimes:
    - name: python  # Invalid: not a supported runtime
      version: "3.9"
      
    - name: ruby  # Invalid: not a supported runtime
      version: "3.0"
      
    - name: node  # Valid runtime
      version: "18.0.0"

workflow:
  steps:
    - id: run_script
      run: echo "Testing runtime validation"