version: "1.0"
metadata:
  name: invalid-provider-test
  description: Test workflow with invalid agent providers

agents:
  agent1:
    provider: google  # Invalid provider
    model: gemini-pro
    
  agent2:
    provider: azure  # Invalid provider
    model: gpt-4
    
  agent3:
    provider: anthropic  # Valid provider
    model: claude-3-haiku-20240307

workflow:
  steps:
    - id: step1
      agent: agent1
      prompt: "Test prompt"