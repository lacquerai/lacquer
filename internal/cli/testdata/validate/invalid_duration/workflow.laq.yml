version: "1.0"
metadata:
  name: invalid-duration-test
  description: Test workflow with invalid duration formats

agents:
  timer:
    provider: anthropic
    model: claude-3-haiku-20240307
    policies:
      timeout: "invalid"  # Invalid duration format - will fail during YAML parsing

workflow:
  steps:
    - id: step1
      agent: timer
      prompt: "Test step"